# Frontend — copy to `.env.local`
NEXT_PUBLIC_API_URL=http://localhost:8000

# Local backend (optional if not using Docker for API)
# DATABASE_URL=postgresql+psycopg://invest_nl:invest_nl_dev@localhost:5432/invest_nl
# CORS_ORIGINS=http://localhost:3000

# Recommended: run DB + API with Docker (Python 3.14 on the host cannot install pydantic wheels):
#   npm run docker:up
# Then: npm run dev → http://localhost:3000  |  API http://localhost:8000  |  Docs http://localhost:8000/docs
# Framework JSON: GET http://localhost:8000/api/framework
